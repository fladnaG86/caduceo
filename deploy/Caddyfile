# Caduceo Relay - Caddy reverse proxy (sviluppo locale)
#
# In produzione il traffico passa tramite Cloudflare Tunnel:
#   client → Cloudflare → cloudflared → localhost:8443
#
# Questo Caddyfile serve solo per test locali sulla porta 8400.

:8400 {
	reverse_proxy localhost:8443
}